classDiagram
direction TB
    class Administrador {
	    -id: int
	    -cedula: String
	    -correo: String
	    -contrasena: String
	    -nombre: String
	    +registrarVeterinario(veterinario: Veterinario)
	    +actualizarVeterinario(veterinario: Veterinario)
	    +cambiarEstadoVeterinario(id: int, estado: String)
	    +generarDashboard() : Dashboard
	    +calcularVentasTotales() : float
	    +calcularGananciasTotales() : float
	    +obtenerTop3Tratamientos() : List~Droga~
    }

    class Dueno {
	    -id: int
	    -cedula: String
	    -nombre: String
	    -correo: String
	    -contrasena: String
	    -celular: String
	    -estado: String
	    +consultarMascotas() : List~Mascota~
	    +actualizarDatos()
	    +activar()
	    +desactivar()
	    +consultarEstado()
    }

    class Veterinario {
	    -id: int
	    -cedula: String
	    -contrasena: String
	    -especialidad: String
	    -numeroAtenciones: int
	    -nombre: String
	    -estado: String
	    -urlFoto: String
	    +registrarDueno(dueno: Dueno)
	    +actualizarDueno(dueno: Dueno)
	    +eliminarDueno(id: int)
	    +registrarMascota(mascota: Mascota)
	    +actualizarMascota(mascota: Mascota)
	    +buscarMascota(nombre: String) : Mascota
	    +cambiarEstadoMascota(id: int, estado: String)
	    +darTratamiento(mascota: Mascota, droga: Droga) : Tratamiento
	    +consultarHistorial(mascota: Mascota) : List~Tratamiento~
	    +cargarMedicamentos(archivo: File)
    }

    class Tratamiento {
	    -id: int
	    -fecha: Date
	    +crear()
	    +consultarDetalle()
    }

    class Droga {
	    -id: int
	    -nombre: String
	    -precioCompra: float
	    -precioVenta: float
	    -unidadesDisponibles: int
	    -unidadesVendidas: int
	    +actualizarInventario(unidades: int)
	    +venderUnidad()
	    +hayDisponibilidad() : boolean
	    +descontarStock(unidades: int)
	    +incrementarVentas(unidades: int)
    }

    class Mascota {
    }

    Administrador "1" -- "0..*" Veterinario : gestiona
    Dueno "1" *-- "0..*" Mascota : posee
    Veterinario "1" -- "0..*" Tratamiento : realiza
    Mascota "1" -- "0..*" Tratamiento : recibe
    Tratamiento "0..*" -- "1" Droga : utiliza